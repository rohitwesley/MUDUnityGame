using UnityEngine;
public class WallTile : EdgeTile
{
	/*public Transform wall;

	public override void Initialize(LevelTile cell, LevelTile otherCell, WalkDirection direction)
	{
		base.Initialize(cell, otherCell, direction);
		wall.GetComponent<Renderer>().material = cell.room.settings.wallMaterial;
	}*/

}
